import React from 'react'

export const NavPanel = (props) => {

    return (

    // Dropdown list for navigation content.
        // Show list of navigational content for Anon & Registered users.
    )
}